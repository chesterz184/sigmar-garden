{
  "name": "sigma-garden",
  "version": "0.0.1",
  "description": "A mini game from Opus Magnum",
  "main": "index.html",
  "scripts": {
    "build": "babel src -d lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "chaz",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
